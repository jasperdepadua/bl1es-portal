/** Human-readable label for each `Profile.role` value — shared across the portal shell's topbar
 * and any feature page that displays a user's role (e.g. Settings). */
export const ROLE_LABELS = {
  superadmin: 'Principal',
  admin: 'Teacher',
  normal: 'Student',
} as const
