# Secrets for local scripts (e.g. scripts/seed-superadmin.ts).
# Copy to .env.seed.local (git-ignored via *.local) and fill in. NEVER commit real values.
# These are SERVER-ONLY — do not VITE_-prefix them; they must never reach the client bundle.
SUPABASE_URL=https://kdzkydshyibxpquvjcjy.supabase.co
SUPABASE_SECRET_KEY=
SEED_PASSWORD=
SEED_CONTACT_EMAIL=
# optional overrides:
# SEED_USERNAME=principal
# SEED_FIRST=
# SEED_LAST=
